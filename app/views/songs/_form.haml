- title @song.title

= form_for @song, :url => @song.new_record? ? artist_path(@artist) : song_path(@artist, @song)  do |form|
  = form.fieldset do |fields|
    = fields.field :name
    = fields.field :data, :as => :text
  = form.submit